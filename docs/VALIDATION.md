# V0.7 公开验收摘要

本页只记录可公开的机械结果。原始本机日志、窗口坐标、任务名称、进程号、绝对路径、设置文件和运行收据未进入仓库。

## 已验证

- Python 3.14：`--self-test` 33/33 PASS，`--qa-report` PASS。
- Python 3.12：`--self-test` 33/33 PASS，`--qa-report` PASS。
- 两套皮肤共 18 个显示状态均为 152×152、二值 Alpha，且有非空轮廓。
- 后台音频队列投递不阻塞 Tk 主线程，验收期间队列丢弃数为 0。
- 撞墙处理单帧低于 16ms；两套皮肤待机差分切换低于 1ms。
- 公开预览图只包含两套皮肤的渲染状态，不包含桌面截图或个人数据。

## 性能快照

| Python | 撞墙处理 | 碰撞音频投递 | 点击音频投递 | 最慢待机差分切换 |
| --- | ---: | ---: | ---: | ---: |
| 3.14 | 1.410ms | 0.024ms | 0.078ms | 0.351ms |
| 3.12 | 1.754ms | 0.021ms | 0.073ms | 0.421ms |

这些数值来自一台 Windows 测试机，只用于证明热路径没有同步音频阻塞，不代表所有设备的性能保证。

## 用户验收

V0.7 的皮肤切换、音效选择、音量、待机差分、拖拽甩出、跟随、缩放、位置预设和状态气泡已完成一轮人工试用，未发现明显阻断问题。

## 未声称

- 未验证所有 Windows 缩放比例、多显示器排列、辅助功能或音频驱动组合。
- Codex 内部数据接口若在未来版本变化，任务或额度显示可能降级为未知；桌宠交互不应因此停止。
- 本摘要不是角色、商标或美术素材的权利证明。
